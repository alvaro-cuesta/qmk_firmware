#pragma once

#undef CAPS_LOCK_LED_INDEX
#define CAPS_LOCK_LED_INDEX 60 // normal caps lock key index

#define NUM_LOCK_LED_INDEX 37 // normal num lock key index

#define SCROLL_LOCK_LED_INDEX 14 // scroll lock key index -- positioned where it usually is in other keyboards, microphone key in V6 (Siri/Cortana)

#define NKRO_LED_INDEX 85 // N key

#define FORCE_TURN_OFF_INDICATORS // force turning off indicators when RGB effects are on
